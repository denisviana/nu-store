import 'package:flutter/material.dart';

class AppBorderRadius {
  static BorderRadius extraMini = BorderRadius.circular(2);
  static BorderRadius mini = BorderRadius.circular(4);
  static BorderRadius tiny = BorderRadius.circular(8);
  static BorderRadius small = BorderRadius.circular(10);
  static BorderRadius medium = BorderRadius.circular(15);
  static BorderRadius large = BorderRadius.circular(20);
  static BorderRadius mega = BorderRadius.circular(25);
}
