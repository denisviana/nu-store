import 'dart:ui';

class AppCornerRadius {
  static const Radius extraMini = Radius.circular(2);
  static const Radius mini = Radius.circular(4);
  static const Radius tiny = Radius.circular(8);
  static const Radius small = Radius.circular(10);
  static const Radius medium = Radius.circular(15);
  static const Radius large = Radius.circular(20);
  static const Radius mega = Radius.circular(25);
}
